/*document.body.addEventListener('mouseup')

function startDrag(el) {
    el.addEventListener('mousedown',(event) => {
        $dragEl = el;
isDown = true;
console.log('mousedown');
offset.x = $dragEl.offsetLeft - event.clientX;
offest.y = $dragEl.offestTop - event.clientY;
$dragEl.style.zIndex =
    })
}
*/